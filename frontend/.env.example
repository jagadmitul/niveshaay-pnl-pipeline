# Copy to .env.local. The dev server proxies /api/process-pdf here.
VITE_N8N_BASE_URL=http://localhost:5678

# Optional: bypass the proxy and hit a remote webhook directly.
# VITE_N8N_WEBHOOK_URL=https://your-n8n-host/webhook/process-pdf
